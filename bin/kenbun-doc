#!/usr/bin/env bash
# ==============================================================================
# 📜 KENBUN-DOC: The Forensic Historian
# Post-Mortem & Hivemind Memory Archaeologist
# ==============================================================================
set -e

DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

if [ -f "${DIR}/venv/bin/python" ]; then
    PYTHON_BIN="${DIR}/venv/bin/python"
elif [ -f "${DIR}/.venv/bin/python" ]; then
    PYTHON_BIN="${DIR}/.venv/bin/python"
elif command -v python3 >/dev/null 2>&1; then
    PYTHON_BIN="python3"
else
    PYTHON_BIN="python"
fi

if [ -z "$1" ]; then
    echo -e "\033[1m\033[36m📜 KENBUN-DOC — The Forensic Historian\033[0m"
    echo -e "Usage:"
    echo -e "  bin/kenbun-doc \"<query_or_incident>\""
    exit 0
fi

export PYTHONPATH="${DIR}:${DIR}/core:${PYTHONPATH:-}"
export PROJECT_ROOT="${DIR}"

exec "$PYTHON_BIN" -c "
import sys
import json
from tools.specialists.pit_crew import consult_kenbun_doc

query = sys.argv[1]

res = json.loads(consult_kenbun_doc(query=query))

print(f'\033[1m\033[36m📜 [KENBUN-DOC ARCHIVE REPORT]\033[0m Verdict: {res.get(\"verdict\")}')
print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━')
recs = res.get('records', [])
if not recs:
    print('ℹ️ No historical post-mortem entries matched this query.')
else:
    for i, r in enumerate(recs, 1):
        print(f'[{i}] Source: {r.get(\"source\")}')
        print(r.get(\"snippet\").strip())
        print('- - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -')
print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━')
" "$@"
